package com.example.calldetect;

public class Mini_documentation  {
    /*
     * TODO
     *      CACHED_LOOKUP_URI : L'URI mis en cache pour rechercher le contact associé au numéro de téléphone, s'il existe.
     *      CACHED_MATCHED_NUMBER : Le numéro de téléphone mis en cache du contact qui correspond à cette entrée, s'il existe.
     *      CACHED_NORMALIZED_NUMBER : La version normalisée d'un numero de telephone mis en cache, si elle existe.
     *tous les numero dans un telphone qui nauront pas le code du pays , on rajoutera le code de leurs pays derriere !
     * dans ce cas, on  ne fonctionnera uniquement qu'avec les numero complet et conforme !
     *
     *
     *
     *
     */
}
